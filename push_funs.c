#include "monty.h"

/**
  *pop_top - add node to the top of stack
  *@stack: pointer to pointer to the first node of stack
  *@line_num: count of line number of the opcode
 */

/*void pop_top(stack_t, **top; unsigned int line_num)
**{
**	stack_t *temp;
**
**	if (top == NULL || *top == NULL)
}*/
